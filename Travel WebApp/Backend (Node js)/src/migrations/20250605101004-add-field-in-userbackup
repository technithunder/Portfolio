'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  async up (queryInterface, Sequelize) {
    await queryInterface.addColumn('User','adharCard', {
      type: Sequelize.STRING,
      allowNull: true,
    })
    await queryInterface.addColumn('User','panCard', {
      type: Sequelize.STRING,
      allowNull: true,
    })
  },

  async down (queryInterface, Sequelize) {
    await queryInterface.removeColumn('User','adharCard')
    await queryInterface.removeColumn('User','panCard')
  }
};
