"use client";
import React from "react";
import { Typography } from "@mui/material";
import Layout from "@/Layout";

const Setting = () => {
  return (
    <Layout>
      <Typography variant="h3">Welcome setting!</Typography>
    </Layout>
  );
};

export default Setting;
